const AlimentoSelecionado = 'Arroz'

switch (AlimentoSelecionado) {
        case 'picanha':
            console.log('Vamos jantar ' + AlimentoSelecionado)
            break;
        case 'churrasco':
            console.log('Vamos jantar ' + AlimentoSelecionado)
            break;
        default:
            console.log('O item nao consta no cardápio')
            break;
}  